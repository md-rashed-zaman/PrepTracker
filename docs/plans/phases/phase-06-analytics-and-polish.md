# Phase 06: Analytics + Polish (2-4 days)

Goal: dashboards give actionable signals and UX is “daily-usable”.

## Deliverables
- Stats endpoints:
  - overview counts (due today, overdue, due soon)
  - mastery per topic
  - streak + consistency
- UI:
  - Stats page charts/tables (start simple)
  - better search/filter in Library
  - empty states + error states

## Testing
- Snapshot-style tests for metric computations (pure functions).
- Minimal e2e smoke (Playwright optional) for core flows.

## Exit criteria
- Acceptance criteria Milestone 4 from `AGENTS.md` passes end-to-end.

